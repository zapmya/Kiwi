class Test {

    void main() {

        int4 x = new int4 ( 0,1,2,3 );
        int4 y = new int4 ( 7,8,9,6 );

        // START: add proper validation for this case
        //boolean4 cmp0 = x < y[0];
        // END: add proper validation for this case

        boolean4 cmp = x < y;
        if ( cmp[0] ) {
            print4(x);
        }
    }

    extern void print4(int4 x);
}
